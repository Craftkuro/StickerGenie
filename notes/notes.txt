一次只打开一个图库
导入的时候一样，只能导入到图库
数据库迁移到高版本怎么办
数据库在另一个线程，包装的时候指定函数，为了测试方便，需要加一个sync=true的选项来直接返回结果
ui调用时需要指定callback，利用signal/slot机制来获取结果

数据库
>>图片
id
存储路径
原始文件名
导入时间
图片尺寸
hash
向量库id
图中文字
仅普通实例：tags

>>tag
id
名称
描述
显示颜色
是否启用
顺序值（以后再实现）


取消main tag设计，改为给tag设置颜色然后指定不同颜色的Tag的显示顺序，默认值是0，同顺序值的tag按字母顺序排序



