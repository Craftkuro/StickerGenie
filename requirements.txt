# Image Features Extractor 模块依赖
# 
# 本文件列出了 image_features_extractor 模块所需的所有依赖项
# 安装方法: pip install -r requirements.txt

# ============================================================================
# 核心依赖
# ============================================================================

# 数组操作和特征向量存储
numpy>=1.24.0

# 图像加载和预处理
Pillow>=10.0.0

# ONNX 模型推理 (CPU 版本)
onnxruntime>=1.16.0

# ONNX 模型推理 (GPU 版本；同时包含 CPU provider，可回退 CPU)
# onnxruntime-gpu[cuda,cudnn]>=1.28.0

# 余弦相似度计算(用于图像相似度查找)
scipy>=1.11.0

# PyQt6 信号系统和UI集成
PyQt6>=6.5.0

# 向量数据库
chromadb>=1.5.9,<2.0

# SQLAlchemy ORM（用于 StickerDB V1）
sqlalchemy>=2.0.0

# TOML 配置文件解析（保持注释）
tomlkit>=0.12.0

# ============================================================================
# 可选依赖 (提升性能)
# ============================================================================

# 如果需要更快的图像处理，可以安装:
# opencv-python>=4.8.0

# ============================================================================
# 开发依赖 (仅用于开发和测试)
# ============================================================================

# 单元测试框架
# pytest>=7.4.0
# pytest-qt>=4.2.0
# pytest-timeout>=2.1.0
# pytest-cov>=4.1.0

# 仅用于验证 Pillow/NumPy 预处理与官方 ViT transform 数值一致；运行时不需要
# torchvision>=0.16.0

# 代码质量检查
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.5.0
