# StickerGenie 直接依赖（只记录一级依赖，传递依赖由 pip 自动安装）
# 安装方法: pip install -r requirements.txt

# ============================================================================
# 运行时依赖
# ============================================================================

# 数组操作和特征向量存储
numpy==2.4.6

# 图像加载和预处理
Pillow==12.2.0

# ONNX 模型推理（CPU 版本；opset 18 需要 1.17+）
onnxruntime==1.28.0

# OCR 提取图中文本（RapidOCR 默认打包 PP-OCRv6 small，覆盖中英日）
rapidocr==3.9.2

# PyQt6 信号系统和 UI 集成
PyQt6==6.11.0

# 向量数据库
chromadb==1.5.9

# SQLAlchemy ORM（用于 StickerDB V1）
SQLAlchemy==2.0.50

# 布尔表达式解析（用于高级标签搜索）
boolean.py==5.0

# TOML 配置文件解析（保持注释）
tomlkit==0.15.0

# ============================================================================
# 模型导出 / 测试参考依赖（应用运行时不需要）
# ============================================================================

#torch==2.12.0
#torchvision==0.27.0
#transformers==5.15.0
#onnx==1.22.0
#onnxscript==0.7.1

# 代码质量检查
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.5.0


# ============================================================================
# 打包依赖
# ============================================================================

pyinstaller==6.21.0

# ============================================================================
# 其他开发依赖
# ============================================================================

# 如果需要Qt Widgets Designer，可以安装pyside6-utils，不过这样也会带来pyside6及相关依赖

