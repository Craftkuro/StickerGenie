# Image Features Extractor 模块依赖
# 
# 本文件列出了 image_features_extractor 模块所需的所有依赖项
# 安装方法: pip install -r requirements.txt

# ============================================================================
# 核心依赖
# ============================================================================

# 数组操作和特征向量存储
numpy>=1.24.0

# 图像加载和预处理
Pillow>=10.0.0

# ONNX 模型推理 (CPU 版本；opset 18 需要 1.17+)
onnxruntime>=1.17.0

# ONNX 模型推理 (GPU 版本；同时包含 CPU provider，可回退 CPU)
# onnxruntime-gpu[cuda,cudnn]>=1.28.0

# OCR 提取图中文本（RapidOCR 默认打包 PP-OCRv6 small，覆盖中英日）
rapidocr>=3.9.2

# 余弦相似度计算(用于图像相似度查找)
scipy>=1.11.0

# PyQt6 信号系统和UI集成
PyQt6>=6.5.0

# 向量数据库
chromadb>=1.5.9,<2.0

# SQLAlchemy ORM（用于 StickerDB V1）
sqlalchemy>=2.0.0

# TOML 配置文件解析（保持注释）
tomlkit>=0.12.0

# ============================================================================
# 可选依赖 (提升性能)
# ============================================================================

# 如果需要更快的图像处理，可以安装:
# opencv-python>=4.8.0

# ============================================================================
# 开发依赖 (仅用于开发和测试)
# ============================================================================

# 单元测试框架
# pytest>=7.4.0
# pytest-qt>=4.2.0
# pytest-timeout>=2.1.0
# pytest-cov>=4.1.0

# 仅用于验证 Pillow/NumPy 预处理与官方 ViT transform 数值一致；运行时不需要
# torchvision>=0.16.0
#
# 生成 DINOv2 ONNX（src/misc/export_dinov2_vitb14_onnx.py）时需要：
# torch>=2.0.0、torchvision>=0.15.0、onnx>=1.14.0、onnxscript
#
# 生成 SigLIP ONNX（src/misc/export_siglip_onnx.py）时需要：
# torch、transformers>=4.37、safetensors、onnx、onnxruntime
# 并提供一个本地 HuggingFace siglip-base-patch16-224 模型目录

# 代码质量检查
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.5.0
